= form_for @product do |f|
  -if @product.errors.any?
    #error_explanation
      %h2= "#{pluralize(@product.errors.count, "error")} prohibited this product from being saved:"
      %ul
        - @product.errors.full_messages.each do |msg|
          %li= msg  
       

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :price
    = f.text_field :price
  .field
    = f.label :quantity
    = f.number_field :quantity
  .field
    = f.label :description
    = f.text_area :description, :rows => 40, :cols => 220
  .actions
    = f.submit 'Save'
                                  
    / http://fiddle.tinymce.com/niaaab   
    
    :javascript  
      $(function() {
        $('textarea').tinymce({
          theme: "advanced",   
          plugins : "preview,table",
          theme_advanced_buttons1 : "preview,|,bold,italic,underline,strikethrough,|,justifyleft,justifycenter,justifyright,justifyfull,|,formatselect,fontselect,fontsizeselect",
          skin : "o2k7",
          width: '70%',
          skin_variant : "silver",
          theme_advanced_toolbar_location : 'top',
          theme_advanced_toolbar_align : "left", 
          theme_advanced_toolbar_location : "external",
          theme_advanced_resizing : true
        });
      });
      